# Core database connection (PostgreSQL)
DATABASE_URL=postgres://USER:PASSWORD@HOST:PORT/DATABASE

# Express session + cookies
SESSION_SECRET=change-me

# Authentication provider (defaults point to Replit's OIDC in dev)
ISSUER_URL=https://replit.com
REPLIT_DOMAINS=replit.com

# OpenAI key for image extraction (optional for local dev)
OPENAI_API_KEY=
